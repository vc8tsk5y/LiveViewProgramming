Clerk.markdown(
    Text.fillOut(
        """
        # Minecraft Klon

        _Tim Zechmeister_, _Technische Hochschule Mittelhessen_

        _5442812_

        ## how to play
        in der Jshell nach der initialisierung des lvp projekts kann `Game game = new Game()` zum initialisieren des Spiels genutzte werden
        ```java
        $(test)
        ```
        """, Map.of(
            "test", Text.cutOut("./Doc.java", "// test")
        )
    )
);

// test
int arnst = 0;
// test
